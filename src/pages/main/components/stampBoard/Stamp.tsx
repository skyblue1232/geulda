import { cn } from '@/shared/lib';

export interface StampProps {
  isStamp: boolean;
  className?: string;
}

export default function Stamp({ isStamp, className }: StampProps) {
  return <div></div>;
}
